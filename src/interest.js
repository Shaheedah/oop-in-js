//assignment 3
class Interest{
	constructor(fees, balance, interest){
		this.fees = fees;
		this.balance = balance;
		this.interest = interest; 
	};

	finalBalance(){
		let interestEarned, calcBalance;
		interestEarned = this.balance 
	};
};

let interest = new Interest(50, 1000, 12);
interest.finalBalance()